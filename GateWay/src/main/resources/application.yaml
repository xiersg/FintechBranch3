server:
  port: 10010

spring:
  application:
    name: GateWay
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}  # 默认dev，命令行可覆盖

  cloud:
    nacos:
      discovery:
        server-addr: ${wx.nacos.host}:${wx.nacos.port}
        # 强制指定 gRPC 地址
        server-grpc-addr: ${wx.nacos.host}:${wx.nacos.gRPC}

    gateway:
      httpclient:
        connect-timeout: 60000 # 连接超时时间，单位毫秒
        response-timeout: 60000 # 响应超时时间，单位毫秒
      # 全局配置
      # 配置全局跨域
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:10010"
            allowedMethods:
              - "GET"
              - "POST"
              - "PUT"
              - "DELETE"
              - "OPTIONS"
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 36000
      routes:
        # 登录认证服务
        - id: AuthenticationService
          uri: lb://AuthenticationService
          # uri: http://localhost:8082
          predicates:
            - Path=/user/**
            # - After=2026-01-20T17:42:47.789-07:00[America/Denver]
          filters:
            - AddRequestHeader=X-Request-Source, InfiniteChat-GateWay
            - AddResponseHeader=X-Response-Source, InfiniteChat-GateWay

        #  管理用户服务
        - id: ManageUserService
          uri: lb://ManageUserService
          predicates:
            - Path=/admin/**

        #  投诉服务
        - id: ComplaintService
          uri: lb://ComplaintService
          predicates:
            - Path=/complaint/**

        #  AI服务
        - id: AiService
          uri: lb://AiService
          predicates:
            - Path=/ai/**, /session/**

#        - id: websocket-service
#          uri: ws://127.0.0.1:6666
#          predicates:
#              - Path=/ws/**


wuxuan:
  auth:
    exclude-paths:
      - /user/login
      - /user/loginCode
      - /user/register
      - /user/common/getCode
      - /user/changePassword